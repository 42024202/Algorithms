#include <stdio.h>


int binary_search(int arr[], int target)
{
    int low, high, mid;
    high = sizeof(arr)/sizeof(int);
    low = 0;

    for(low = 0, low <= high; mid = (low + high) / 2)
    {
        
    }
}
